<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Order Confirmation Satchel</title>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet">
  <style>
    :root {
      --primary: #8a5a44;
      --primary-light: #c9b6a8;
      --secondary: #274546;
      --accent: #d4af37;
      --light: #f8f5f2;
      --dark: #2d2a2a;
      --success: #4a9375;
      --pending: #e7bb41;
      --danger: #d9534f;
      --info: #5bc0de;
      --white: #ffffff;
      --gray: #777;
      --border: #eee;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Poppins', sans-serif;
      background-color: var(--light);
      color: var(--dark);
      line-height: 1.6;
    }

    .container {
      max-width: 1000px;
      margin: 0 auto;
      padding: 0 20px;
    }

    .confirmation-container {
      background-color: var(--white);
      border-radius: 12px;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
      margin: 40px auto;
      padding: 40px;
      max-width: 850px;
      position: relative;
      overflow: hidden;
    }

    .success-icon {
      width: 90px;
      height: 90px;
      background-color: var(--success);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 30px;
      animation: pulse 2s infinite;
    }

    .success-icon i {
      color: var(--white);
      font-size: 48px;
    }

    @keyframes pulse {
      0% { transform: scale(1); }
      50% { transform: scale(1.1); }
      100% { transform: scale(1); }
    }

    .thank-you {
      text-align: center;
      margin-bottom: 40px;
    }

    .thank-you h1 {
      font-family: 'Playfair Display', serif;
      font-size: 40px;
      margin-bottom: 10px;
      color: var(--secondary);
    }

    .thank-you p {
      color: var(--gray);
      font-size: 18px;
    }

    .order-details {
      padding: 30px;
      border: 1px solid var(--border);
      border-radius: 8px;
      margin-bottom: 30px;
      background-color: #fdfdfd;
    }

    .section-title {
      font-family: 'Playfair Display', serif;
      color: var(--primary);
      font-size: 24px;
      margin-bottom: 20px;
      padding-bottom: 10px;
      border-bottom: 2px solid var(--primary-light);
      position: relative;
    }

    .section-title::after {
      content: '';
      position: absolute;
      left: 0;
      bottom: -2px;
      width: 70px;
      height: 2px;
      background-color: var(--accent);
    }

    .detail-row {
      display: flex;
      justify-content: space-between;
      padding: 12px 0;
      border-bottom: 1px dashed var(--border);
    }

    .detail-row:last-child {
      border-bottom: none;
    }

    .detail-label {
      font-weight: 500;
      color: var(--dark);
    }

    .detail-value {
      color: var(--gray);
      font-weight: 400;
    }

    .status-badge {
      display: inline-block;
      padding: 6px 14px;
      border-radius: 20px;
      font-size: 14px;
      font-weight: 500;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    .status-pending {
      background-color: var(--pending);
      color: #9a7d27;
    }

    .status-processing {
      background-color: var(--info);
      color: var(--white);
    }

    .status-shipped {
      background-color: #5cb85c;
      color: var(--white);
    }

    .status-delivered {
      background-color: var(--success);
      color: var(--white);
    }

    .status-cancelled, .status-returned {
      background-color: var(--danger);
      color: var(--white);
    }

    .ordered-items {
      margin-top: 20px;
    }

    .item-card {
      display: flex;
      align-items: center;
      margin-bottom: 20px;
      padding: 15px;
      background-color: var(--white);
      border-radius: 8px;
      box-shadow: 0 3px 8px rgba(0,0,0,0.06);
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .item-card:hover {
      transform: translateY(-3px);
      box-shadow: 0 6px 15px rgba(0,0,0,0.1);
    }

    .item-image {
      width: 90px;
      height: 90px;
      background-color: #f5f5f5;
      display: flex;
      align-items: center;
      justify-content: center;
      border-radius: 6px;
      margin-right: 20px;
    }

    .item-image img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      border-radius: 6px;
    }

    .item-details {
      flex: 1;
    }

    .item-name {
      font-weight: 600;
      margin-bottom: 8px;
      color: var(--secondary);
      font-size: 16px;
    }

    .item-meta {
      font-size: 14px;
      color: var(--gray);
      display: flex;
      gap: 20px;
    }

    .item-price {
      font-weight: 600;
      color: var(--primary);
      font-size: 16px;
    }

    .buttons {
      display: flex;
      justify-content: center;
      gap: 20px;
      margin-top: 40px;
    }

    .btn {
      display: inline-block;
      padding: 12px 30px;
      border-radius: 50px;
      text-decoration: none;
      font-weight: 500;
      letter-spacing: 0.5px;
      transition: all 0.3s ease;
      border: none;
      cursor: pointer;
      text-align: center;
    }

    .btn-primary {
      background-color: var(--primary);
      color: var(--white);
      box-shadow: 0 4px 15px rgba(138, 90, 68, 0.2);
    }

    .btn-primary:hover {
      background-color: #7a4e3a;
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(138, 90, 68, 0.3);
    }

    .btn-secondary {
      background-color: transparent;
      color: var(--secondary);
      border: 2px solid var(--secondary);
    }

    .btn-secondary:hover {
      background-color: var(--secondary);
      color: var(--white);
      transform: translateY(-2px);
      box-shadow: 0 4px 15px rgba(39, 69, 70, 0.2);
    }

    .order-number {
      font-family: 'Courier New', monospace;
      background-color: #f7f7f7;
      padding: 4px 10px;
      border-radius: 4px;
      font-size: 14px;
      letter-spacing: 0.5px;
    }

    .divider {
      height: 1px;
      background: linear-gradient(to right, transparent, var(--primary-light), transparent);
      margin: 40px 0;
    }

    .price-highlight {
      font-size: 20px;
      color: var(--secondary);
      font-weight: 600;
    }

    .address-details {
      padding: 20px;
      background-color: #f9f9f9;
      border-radius: 8px;
      margin-top: 20px;
    }

    .address-details p {
      margin: 5px 0;
      color: var(--gray);
    }

    @media (max-width: 768px) {
      .confirmation-container {
        padding: 20px;
      }

      .buttons {
        flex-direction: column;
        gap: 15px;
      }

      .item-card {
        flex-direction: column;
        align-items: flex-start;
      }

      .item-image {
        margin-bottom: 15px;
        margin-right: 0;
      }

      .item-meta {
        flex-direction: column;
        gap: 5px;
      }
    }
  </style>
</head>
<body>
  <%- include('../../views/partials/user/header') %>
  
  <% const formatCurrency = (amount) => `₹${parseFloat(amount || 0).toFixed(2)}` %>
  <% const formatDate = (date) => date ? new Date(date).toLocaleDateString('en-IN', { 
      year: 'numeric', month: 'long', day: 'numeric' 
    }) : 'N/A' %>
  <% const getStatusClass = (status) => {
    switch (status?.toLowerCase()) {
      case 'pending': return 'status-pending';
      case 'processing': return 'status-processing';
      case 'shipped': return 'status-shipped';
      case 'delivered': return 'status-delivered';
      case 'cancelled': return 'status-cancelled';
      case 'returned': return 'status-returned';
      default: return 'status-pending';
    }
  } %>
  <% const estimateDeliveryDate = (orderDate) => {
    if (!orderDate) return 'N/A';
    const date = new Date(orderDate);
    date.setDate(date.getDate() + 7); // Estimate delivery within 7 days
    return date.toLocaleDateString('en-IN', { year: 'numeric', month: 'long', day: 'numeric' });
  } %>

  <main class="container">
    <div class="confirmation-container">
      <div class="success-icon" aria-label="Order confirmed">
        <i class="fas fa-check"></i>
      </div>
      
      <div class="thank-you">
        <h1>Thank You for Your Order!</h1>
        <p>We’ve received your order and will process it shortly. You’ll receive a confirmation email soon.</p>
      </div>
      
      <div class="order-details">
        <h3 class="section-title">Order Information</h3>
        <div class="detail-row">
          <span class="detail-label">Order ID:</span>
          <span class="detail-value order-number">ORD<%= order?.orderId || order?._id || 'N/A' %></span>
        </div>
        <div class="detail-row">
          <span class="detail-label">Date:</span>
          <span class="detail-value"><%= formatDate(order?.createOn) %></span>
        </div>
        <div class="detail-row">
          <span class="detail-label">Payment Method:</span>
          <span class="detail-value"><%= order?.paymentMethod ? order.paymentMethod.charAt(0).toUpperCase() + order.paymentMethod.slice(1) : 'N/A' %></span>
        </div>
        <div class="detail-row">
          <span class="detail-label">Status:</span>
          <span class="status-badge <%= getStatusClass(order?.status) %>"><%= order?.status || 'Pending' %></span>
        </div>
        <div class="detail-row">
          <span class="detail-label">Estimated Delivery:</span>
          <span class="detail-value"><%= estimateDeliveryDate(order?.createOn) %></span>
        </div>
      </div>
      
      <div class="order-details">
        <h3 class="section-title">Price Summary</h3>
        <div class="detail-row">
          <span class="detail-label">Subtotal:</span>
          <span class="detail-value"><%= formatCurrency(order?.subTotal) %></span>
        </div>
        <% if (typeof order?.discount !== 'undefined' && order?.discount > 0) { %>
        <div class="detail-row">
          <span class="detail-label">Discount:</span>
          <span class="detail-value"><%= formatCurrency(order?.discount) %></span>
        </div>
        <% } %>
        <div class="detail-row">
          <span class="detail-label">Final Amount:</span>
          <span class="detail-value price-highlight"><%= formatCurrency(order?.finalAmount) %></span>
        </div>
      </div>
      
      <div class="order-details">
        <h3 class="section-title">Ordered Items</h3>
        <div class="ordered-items">
          <% if (order?.items && Array.isArray(order.items) && order.items.length > 0) { %>
            <% order.items.forEach(item => { %>
              <div class="item-card">
                <div class="item-image">
                  <% if (item?.productId && item.productId.productImage && Array.isArray(item.productId.productImage) && item.productId.productImage.length > 0) { %>
                    <img 
                              src="<%= item?.productId?.productImage && Array.isArray(item.productId.productImage) && item.productId.productImage.length > 0 ? item.productId.productImage[0] : 'https://via.placeholder.com/50' %>" 
                              alt="<%= item?.productId?.productName || 'Product' %>" 
                              class="w-12 h-12 rounded mr-3 border border-gray-300"
                              onerror="this.onerror=null; this.src='https://via.placeholder.com/50';"
                            >
                  <% } else { %>
                    <i class="fas fa-box" aria-hidden="true"></i>
                    <span class="sr-only">No image available</span>
                  <% } %>
                </div>
                <div class="item-details">
                  <div class="item-name"><%= item?.productId?.productName || 'Unnamed Item' %></div>
                  <div class="item-meta">
                
                    <div>Quantity: <%= item?.quantity || '1' %></div>
                  </div>
                </div>
                <div class="item-price">
                  <%= formatCurrency((item?.price || 0) * (item?.quantity || 1)) %>
                </div>
              </div>
            <% }); %>
          <% } else { %>
            <div>No Ordered Items Found</div>
          <% } %>
        </div>
      </div>

    
      
      <div class="divider"></div>
      
      <div class="buttons">
        <a href="/userProfile/order-details/<%= order?.orderId || '' %>" class="btn btn-primary">View Order Details</a>
        <a href="/shop" class="btn btn-secondary">Continue Shopping</a>
      </div>
    </div>
  </main>
  
  <%- include('../../views/partials/user/footer') %>
</body>
</html>